project:
  name: "{{ .Name }}"

simulation:
  deterministic: true
  tick_rate: 60
  rollback: true

backend:
  execution: bits
  scheduler: graph

network:
  replication: delta

components:
  default_capacity: 4096
